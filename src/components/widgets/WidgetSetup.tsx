import { TWidget } from "../../types";

type TWidgetSetupProps = {
  onSave: (widget: TWidget) => void;
};

const WidgetSetup = ({}: TWidgetSetupProps) => {
  return <div></div>;
};

export { WidgetSetup };
